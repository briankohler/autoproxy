AUTOMATIC HAPROXY

A small ruby wrapper around haproxy that watches for services defined as yaml files in a folder and updates/hups haproxy as configs change.  You can use consul template to place these yaml files in a dynamic way.  
